import { Typography } from '@/ui/design-system/typography';

export default function Home() {
  return (
    <main>
      <Typography variant={'h2'} component={'h2'}>
        hello
      </Typography>
      <Typography variant={'h2'} component={'h2'}>
        Je
      </Typography>
      <Typography variant={'h2'} component={'h2'}>
        suis
      </Typography>
      <Typography variant={'h2'} component={'h2'}>
        Benjamin
      </Typography><Typography variant={'h2'} component={'h2'}>
      Guelle
    </Typography>

    </main>
  );
}
