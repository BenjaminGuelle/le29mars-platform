/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    // reactStrictMode: true,
    // images: { // en cas de probleme de build sur d'optimisation d'image
    //     unoptimized: true,
    // }
};

export default nextConfig;
